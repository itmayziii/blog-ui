import { SlugTitleDirective } from './slug-title.directive';

describe('SlugTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new SlugTitleDirective();
    expect(directive).toBeTruthy();
  });
});
